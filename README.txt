# 🎮 Roguelike Test Game (PgZero)

Este projeto é um jogo **roguelike 2D** desenvolvido em **Python** utilizando **PgZero**, criado exclusivamente para fins educacionais como parte de um **teste técnico para tutores**.

O objetivo do jogo é coletar todas as moedas espalhadas pelo mapa enquanto evita inimigos que se movimentam em áreas limitadas. O jogo possui menu principal, música, sons, animações de sprites e mecânicas simples, porém bem definidas.

---

## 📋 Requisitos do Sistema

### Sistema Operacional
- Windows 10 ou superior
- Linux (Ubuntu 20.04 ou superior)
- macOS 10.15 ou superior

> ⚠️ O jogo não é compatível com dispositivos móveis.

---

## 🐍 Requisitos de Software

### Python
- **Python 3.9, 3.10 ou 3.11** (recomendado)
- Python 3.12 **não recomendado** para PgZero

🔗 Download oficial:  
https://www.python.org/downloads/

> ⚠️ No Windows, marque a opção **"Add Python to PATH"** durante a instalação.

---

## 📦 Bibliotecas Utilizadas

Somente as seguintes bibliotecas são usadas no projeto, conforme exigido no teste:

- `pgzero` (≥ 1.2)
- `pygame` (≥ 2.1) — usado **apenas** para a classe `Rect`
- `math` (biblioteca padrão do Python)
- `random` (biblioteca padrão do Python)

### Instalação das dependências
```bash
pip install pgzero pygame

## ▶️ Como Rodar o Jogo

Antes de executar o jogo, certifique-se de que o **Python** e as **dependências** estão corretamente instalados.

### 1️⃣ Instalar as dependências

No terminal ou prompt de comando, execute:

```bash
pip install pgzero pygame
